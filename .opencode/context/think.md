# Guide to using the `think` MCP server

- Treat the `think_sequentialthinking_tools` as **reasoning aids**, not as a place to store tasks or long-term project state.
- Use them to **plan your actions and choose relevant tools** in order to accomplish your goals.

Do **not** use `think` for:

- Small, obvious tasks where the next action is trivial.
